{% extends "paragraph.html.twig" %}
    {% block content %}
      <div class="image">
      {{ content.field_image }}
      </div>
      <div class="content-wrapper">
        {{ content|without('field_image') }}
      </div>
    {% endblock %}
